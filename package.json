{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.3"
  },
  "scripts": {

    "lint": "lerna run lint",
    "version": "lerna version",
    "build": "lerna run build",

    "list": "lerna list --long",
    "graph": "lerna list --graph",
    "diff": "lerna diff"
    
  }
}
